<Window x:Class="NetshG.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:NetshG"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <Style TargetType="TextBlock" x:Key="rTitle">
            <Setter Property="FontSize" Value="28" />
            <Setter Property="FontWeight" Value="Bold" />
        </Style>
        <Style TargetType="TextBox" x:Key="rOutput">
            <Setter Property="FontFamily" Value="Cascadia Mono" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style TargetType="TextBox" x:Key="rCommand">
            <Setter Property="FontSize" Value="16" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="IsReadOnly" Value="True" />
            <Setter Property="TextWrapping" Value="Wrap" />
        </Style>
        <Style TargetType="TextBlock" x:Key="rLog">
            <Setter Property="FontSize" Value="9" />
        </Style>
    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto" />
            <RowDefinition Height="auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="auto" />
        </Grid.RowDefinitions>
        
        <Menu Grid.Row="0">
            <MenuItem Header="_File" >
                <MenuItem Header="E_xit" Click="OnMenu_File_Exit" />
            </MenuItem>
            <MenuItem Header="_Show" >
                <MenuItem Header="_Common" Tag="#common" Click="OnMenu_Show_Tag" />
                <MenuItem Header="_All" Tag="" Click="OnMenu_Show_Tag" />
                <MenuItem Header="_Wi-Fi" Tag="#wifi" Click="OnMenu_Show_Tag" />
                <MenuItem Header="Show _Help" IsCheckable="True" IsChecked="True" Checked="OnMenu_Show_Help_Check" Unchecked="OnMenu_Show_Help_Uncheck" />
            </MenuItem>
            <MenuItem Header="_Parameters">
                <MenuItem Header="_Common" x:Name="uiMenu_Parameters_Common">
                </MenuItem>
            </MenuItem>
        </Menu>
        <TextBlock Grid.Row="1" Style="{StaticResource ResourceKey=rTitle}">Netsh Gui Interface</TextBlock>

        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="auto" MinWidth="400" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <ListView x:Name="uiCommandList" Grid.Column="0" SelectionChanged="OnSelectCommand">
                <!--- Values are set by MainWindow_Loaded from AllNetshCommands.GetCommands() -->
            </ListView>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>
                <TextBox x:Name="uiCommand" Grid.Row="0" Style="{StaticResource rCommand}" />
                <Grid x:Name="uiReplace" Grid.Row="1">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    <TextBlock x:Name="uiReplaceName" Grid.Column="0"/>
                    <TextBlock x:Name="uiReplaceValue" Grid.Column="1" />
                    <Button Content="&lt;" Grid.Column="2" Click="OnPrevMacro" />
                    <Button Content=">" Grid.Column="3" Click="OnNextMacro" />
                </Grid>
                <ScrollViewer x:Name="uiOutputScroll" Grid.Row="2">
                    <TextBox x:Name="uiOutput" Style="{StaticResource rOutput}" />
                </ScrollViewer>
            </Grid>
        </Grid>
        <TextBlock Grid.Row="3"  x:Name="uiLog" Style="{StaticResource ResourceKey=rLog}"></TextBlock>
    </Grid>
</Window>

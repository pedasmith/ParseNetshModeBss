<UserControl x:Class="NetshG.ReplaceViewEditControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:NetshG"
             mc:Ignorable="d" 
             d:DesignHeight="50" d:DesignWidth="400">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="auto" />
        </Grid.ColumnDefinitions>
        <TextBlock x:Name="uiReplaceName" Grid.Column="0"/>
        <TextBlock x:Name="uiReplaceValue" Grid.Column="1" />
        <Button Content="&lt;" Grid.Column="2" Click="OnPrevMacro" />
        <Button Content=">" Grid.Column="3" Click="OnNextMacro" />
    </Grid>
</UserControl>
